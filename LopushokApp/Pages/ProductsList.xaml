<Page x:Class="LopushokApp.Pages.ProductsList"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:local="clr-namespace:LopushokApp.Pages"
      mc:Ignorable="d"
      d:DesignHeight="450" d:DesignWidth="800"
      Title="ProductList">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <!-- Поиск, сортировка, фильтрация -->
        <StackPanel Orientation="Horizontal" Margin="10" HorizontalAlignment="Left">
            <TextBox x:Name="SearchTxtBox" Width="200" Height="25" Margin="5,0"
                     VerticalAlignment="Center" TextChanged="SearchTxtBox_TextChanged" />

            <ComboBox x:Name="SortCmbBox" Width="200" Height="25" Margin="5,0"
                      VerticalAlignment="Center" SelectionChanged="SortCmbBox_SelectionChanged">
                <ComboBoxItem Content="Наименование (А-Я)" Tag="NameAsc" />
                <ComboBoxItem Content="Наименование (Я-А)" Tag="NameDesc" />
                <ComboBoxItem Content="Цех ↑" Tag="WorkshopAsc" />
                <ComboBoxItem Content="Цех ↓" Tag="WorkshopDesc" />
                <ComboBoxItem Content="Цена (Мин-Макс)" Tag="PriceAsc" />
                <ComboBoxItem Content="Цена (Макс-Мин)" Tag="PriceDesc" />
            </ComboBox>

            <ComboBox x:Name="FilterCmbBox" Width="120" Height="25" Margin="5,0"
                      VerticalAlignment="Center" SelectionChanged="FilterCmbBox_SelectionChanged"
                      DisplayMemberPath="Name">
                <ComboBoxItem Content="Все типы" Tag="AllFC" />
                <ComboBoxItem Content="Три слоя" Tag="FreeSFC" />
                <ComboBoxItem Content="Два слоя" Tag="TwoSFC" />
                <ComboBoxItem Content="Детская" Tag="OneSFC" />
                <ComboBoxItem Content="Супер мягкая" Tag="ChildFC" />
                <ComboBoxItem Content="Один слой" Tag="SuperFC" />
            </ComboBox>

            <Button x:Name="AddProdBtn" Margin="5" Width="200" Padding="3" Content="Добавить продукт"
                    Click="AddProdBtn_Click" />
        </StackPanel>

        <!-- Список товаров -->
        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
            <ItemsControl x:Name="ProdItemsIC" Margin="10"
                          MouseDoubleClick="ProdItemsIC_MouseDoubleClick">
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <StackPanel Orientation="Vertical" />
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>

                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Border BorderBrush="LightGray" BorderThickness="1" CornerRadius="5" Margin="5" Padding="10">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto" />
                                    <ColumnDefinition Width="*" />
                                </Grid.ColumnDefinitions>

                                <Image Source="{Binding Image}" Width="70" Height="70"
                                       Stretch="Uniform" Grid.Column="0" VerticalAlignment="Center" Margin="10" />

                                <StackPanel Grid.Column="1" Margin="10,0,0,0">
                                    <TextBlock>
                                        <Run Text="{Binding TypeProduct.Name}" FontWeight="Bold" FontSize="14" />
                                        <Run Text=" | " />
                                        <Run Text="{Binding Name}" FontWeight="Bold" FontSize="14" />
                                    </TextBlock>
                                    <TextBlock Text="{Binding Articul}" FontSize="12" Margin="0 5" />
                                    <TextBlock Text="{Binding Materials, StringFormat='Материалы: {0}'}"
                                               TextWrapping="Wrap" FontSize="12" Margin="0 2" />
                                </StackPanel>

                                <TextBlock Text="{Binding TotalCost, StringFormat=' {0} руб.'}"
                                           FontWeight="Normal" Grid.Column="2" Foreground="#00CC76"
                                           HorizontalAlignment="Right" VerticalAlignment="Top"
                                           Margin="10,0,0,0" FontSize="14" />
                            </Grid>
                        </Border>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>

        <!-- Панель управления страницами -->
        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Center" Margin="10">
            <Button x:Name="PrevPageBtn" Padding="3" Margin="5,0" Click="PrevPageBtn_Click">
                <Image Source="/Image/130882.png" Width="15" />
            </Button>

            <TextBlock x:Name="PageInfoTxtBlock" Text="" Margin="5,0" VerticalAlignment="Center" />

            <Button x:Name="NextPageBtn" Padding="3" Margin="5,0" Click="NextPageBtn_Click">
                <Image Source="/Image/130882.png" Width="15" RenderTransformOrigin="0.5,0.5">
                    <Image.RenderTransform>
                        <TransformGroup>
                            <ScaleTransform />
                            <SkewTransform />
                            <RotateTransform Angle="180" />
                            <TranslateTransform />
                        </TransformGroup>
                    </Image.RenderTransform>
                </Image>
            </Button>
        </StackPanel>
    </Grid>
</Page>
